I need you to implement a fix for the following issue in this repository.

{{#is_github}}
ISSUE: Fixes #{{source_id}}
{{/is_github}}
{{#is_linear}}
ISSUE: Fixes {{source_id}}
{{/is_linear}}

ISSUE TITLE: {{title}}
DESCRIPTION: {{description}}
LABELS: {{labels}}
{{#has_context}}

ADDITIONAL CONTEXT:
{{context}}
{{/has_context}}

Instructions (follow in order):
1. Verify you are on branch {{branch_name}}.
   - Run: git branch --show-current
   - If branch is not {{branch_name}}, stop and output ISSUE_IMPLEMENT_FAILED:{{callback_id}}
2. Understand the relevant code.
3. Implement the fix with clean, tested code.
4. Commit your changes:
   - git add -A
   - git commit -m "Fix {{source_id}}: {{title}}"
5. Push the branch:
   - git push -u origin {{branch_name}}
6. Do not open a pull request in this step.

When complete, output exactly: ISSUE_IMPLEMENT_COMPLETE:{{callback_id}}
If you cannot complete, output exactly: ISSUE_IMPLEMENT_FAILED:{{callback_id}}
